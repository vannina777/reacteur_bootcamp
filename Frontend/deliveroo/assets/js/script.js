/* const $ = document

window.addEventListener("resize", () => {
    if (window.innerWidth < 1200) {
        const container = $.querySelector(".container")
        container.classList.add("container100")
    }
}) */