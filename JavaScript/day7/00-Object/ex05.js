const keyName = "name";

const user = {
  name: "Brice"
};

// Sans exécuter ce fichier, devinez lequel de ces 3 `console.log` est correct ?

// 1
console.log(user.keyName);

// 2
console.log(user[keyName]);

// 3
console.log(user["keyName"]);

// Vérifiez votre réponse
// Si vous n'aviez pas trouvé, relisez le cours "Accéder dynamiquement à une propriété"
