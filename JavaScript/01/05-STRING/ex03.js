/*
  Objectif de l'exercice :
  Concaténer (c'est-à-dire mettre bout à bout deux chaînes de caractères de manière à en former une troisième) `firstWord` et `secondWord`, le résultat devra être stocké dans une variable `result`.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  Reacteur

  Aide :
  ```
  let name = "Xavier";
  let message = "Je m'appelle " + name;
  ```
*/

const firstWord = "React";
const secondWord = "eur";

// Début de votre code
let result = firstWord + secondWord;
// Fin de votre code

console.log(result); // Doit afficher `Reacteur` dans le terminal
