/*
  Objectif de l'exercice :
  Utiliser la méthode de l'interpolation pour concaténer `firstName` et `lastName`, le résultat devra être stocké dans `fullName`.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  Neil Armstrong

  Aide :
  ```
  let name = "Sofiane";
  let message = `Je m'appelle ${name}`;
  ```
*/

const firstName = "Neil";
const lastName = "Armstrong";
let fullName;

// Début de votre code
fullName = `${firstName} ${lastName}`;
// Fin de votre code

console.log(fullName); // Doit afficher `Neil Armstrong` dans le terminal
