/*
  Objectif de l'exercice : 
  Déclarer la variable non-modifiable (constante) `price` et lui assigner la valeur `99`.
  `price` est une variable qui pourrait par exemple servir à stocker le prix d'un produit. Ce prix ne serait pas modifiable. Vous devrez utiliser `const` et non pas `let` pour déclarer la variable.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  99
*/

// Début de votre code
const price = 99;
// Fin de votre code

console.log(price); // Doit afficher `99` dans le terminal
