/*
  Objectif de l'exercice : 
  Créer une variable `result` et soustraire la valeur de la variable `a` à la variable `b`.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  -8

  Explications concernant le résultat :
  Les nombres peuvent être négatifs.
*/

const a = 10;
const b = 2;

// Début de votre code
let result = b - a;
// Fin de votre code

console.log(result); // Doit afficher `-8` dans le terminal
