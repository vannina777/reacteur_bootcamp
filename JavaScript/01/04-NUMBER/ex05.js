/*
  Objectif de l'exercice : 
  Multiplier `a` et `b`, et conserver le résultat dans la variable `result`.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  6

  Explications concernant le résultat :
  Il est possible d'effectuer une multiplication entre 2 variables, si elles sont toutes les deux de type Number, avec l'opérateur `*`.
*/

const a = 2;
const b = 3;

// Début de votre code
let result = a * b;
// Fin de votre code

console.log(result); // Doit afficher `6` dans le terminal
