/*
  Objectif de l'exercice : 
  Assigner la bonne valeur à la variable `num` pour obtenir le résultat attendu.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  15

  Explications concernant le résultat :
  Il est possible d'effectuer une addition entre 2 variables, si elles sont toutes les deux de type Number, avec l'opérateur `+`.
*/

let num;

// Début de votre code
num = 5;
// Fin de votre code

num = num + 10;
console.log(num); // Doit afficher `15` dans le terminal
