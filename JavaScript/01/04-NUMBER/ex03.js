/*
  Objectif de l'exercice : 
  Additioner `a` et `b`, et conserver le résultat dans la variable `result`.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  30

  Explications concernant le résultat :
  Il est possible de créer de nouvelles variables lorsque vous en avez besoin. Ici nous ne souhaitons pas modifier les constantes `a` et `b`, nous avons donc besoin d'une nouvelle variable que nous avons choisi de nommer `result`.
*/

const a = 10;
const b = 20;
let result;

// Début de votre code
result = a + b;
// Fin de votre code

console.log(result); // Doit afficher `30` dans le terminal
