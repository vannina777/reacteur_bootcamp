/*
  Objectif de l'exercice : 
  Diviser `a` par `b`, et conserver le résultat dans la variable `result`.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  2.5

  Explications concernant le résultat :
  Il est possible de stocker un nombre à virgule (que l'on appelle `Float`) dans une variable.
*/

const a = 5;
const b = 2;

// Début de votre code
let result = a / b;
// Fin de votre code

console.log(result); // Doit afficher `2.5` dans le terminal
