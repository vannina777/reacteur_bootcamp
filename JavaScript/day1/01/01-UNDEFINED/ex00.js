/*
  Objectif de l'exercice : 
  Afficher la valeur de la variable `name` dans le terminal. Vous ne devez pas lui assigner de valeur.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  undefined

  Aides :
  - Comment exécuter un script dans le terminal ?
  Vous devez entrer `node` suivi du chemin vers le fichier à exécuter puis appuyer sur la touche `Enter`.
  Exemple : node "/Users/farid/LeReacteur/fichier.js"

  - Comment afficher la valeur d'une variable ?
  Vous devez appeler la fonction `console.log()` et insérer la variable entre les 2 parenthèses.
  Exemple : console.log(counter);

  - Qu'est-ce que `undefined` ?
  Dans cet exercice, la variable `name` est déclarée mais aucune valeur ne lui est assignée. Elle vaut donc `undefined`.
*/

let name;

// Début de votre code
console.log(name);
// Fin de votre code
