/*
  Objectif de l'exercice : 
  Déclarer la variable non-modifiable (constante) `isFruit` et lui assigner la valeur `false`.
  `isFruit` est une variable qui pourrait par exemple définir si un des produits d'un magasin est un fruit. On le déclarera en constante car cette caractéristique n'est pas censée changer.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  false

  Aide :
  - Comment déclarer une constante ?
  Lorsque vous déclarez une variable avec `const`, vous devez lui assigner une valeur sur la même ligne, contrairement à `let`.
*/

// Début de votre code
const isFruit = false;
// Fin de votre code

console.log(isFruit); // Doit afficher `false` dans le terminal
