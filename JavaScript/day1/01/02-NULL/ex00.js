/*
  Objectif de l'exercice : 
  Assigner la valeur `null` à la variable `attachment`.
  `attachment` est une variable qui pourrait par exemple servir à stocker la pièce jointe d'un email.

  Résultat à afficher dans le terminal avant de passer à l'exercice suivant :
  null

  Aide :
  - Qu'est-ce que `null` ?
  `null` est utile lorsque vous voulez préciser que la valeur d'une variable est inexistante. Dans cet exemple, il n'y a pas de pièce jointe.
*/

let attachment;

// Début de votre code
attachment = null;
// Fin de votre code

console.log(attachment); // Doit afficher `null` dans le terminal
