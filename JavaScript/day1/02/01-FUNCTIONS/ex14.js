// Début de votre code
const multiply = (x, y) => {
  const total = x * y;
  return total;
};
// Fin de votre code

console.log(multiply(2, 3)); // Doit afficher `6` dans le terminal
console.log(multiply(2, 2)); // Doit afficher `4` dans le terminal
console.log(multiply(1, 10)); // Doit afficher `10` dans le terminal
