/*
  Output :
  Accès autorisé
*/

let password;

// Début de votre code
password = "azerty";
// Fin de votre code

if (password === "azerty") {
  // Notez la présence de 3 signes `=` pour effectuer une comparaison
  console.log("Accès autorisé"); // Ce code sera exécuté seulement si la condition est `true`
}
