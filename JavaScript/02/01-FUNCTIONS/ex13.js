const sum = (a, b, c) => {
  // Notez que les noms des variables doivent être uniques. Il ne peut y avoir 2 variables portant le même nom
  return a + b + c;
};

let result;
const num1 = 10;
const num2 = 20;
const num3 = 5;

// Début de votre code
result = sum(num1, num2, num3);
// Fin de votre code

console.log(result); // Doit afficher `35` dans le terminal
