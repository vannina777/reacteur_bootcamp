const isActive = true; // Notez qu'il y a qu'un seul signe égal lors d'une assignation de valeur

// !== signifie "Différent de"
if (isActive !== false) {
  console.log("Message 1");
} else {
  console.log("Message 2");
}

// Question
// Selon-vous, quel message s'affichera dans le terminal ? Message 1
// Après avoir répondu à la question, vérifiez votre réponse en exécutant le script
